name: Descargar múltiple con reintentos + proxy
on:
  schedule:
    - cron: '0 * * * *'          # cada hora
  workflow_dispatch:

jobs:
  download:
    runs-on: ubuntu-latest
    steps:

      # -------------------------------------------------
      # 0️⃣ Instalar herramientas (jq + tinyproxy opcional)
      # -------------------------------------------------
      - name: Instalar jq y tinyproxy
        run: |
          sudo apt-get update -y
          sudo apt-get install -y jq
          # tinyproxy es opcional; descoméntalo si quieres usar proxy HTTP
          sudo apt-get install -y tinyproxy
          sudo sed -i 's/^#Port 8888/Port 8888/' /etc/tinyproxy/tinyproxy.conf
          sudo systemctl restart tinyproxy

      # -------------------------------------------------
      # 1️⃣ Checkout del repositorio (sin persistir credenciales)
      # -------------------------------------------------
      - name: Checkout del repositorio
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          persist-credentials: false   # evita usar GITHUB_TOKEN para push

      # -------------------------------------------------
      # 2️⃣ Estrategia combinada (reintentos + proxy)
      # -------------------------------------------------
      - name: Descargar según JSON (seguro)
        # -----------------------------------------------------------------
        # Si deseas usar el proxy HTTP de tinyproxy (escucha en 127.0.0.1:8888),
        # descomenta las tres líneas siguientes.
        # -----------------------------------------------------------------
        env:
          http_proxy:  http://127.0.0.1:8888
          https_proxy: http://127.0.0.1:8888
        # -----------------------------------------------------------------
        env:
          DOWNLOADS_JSON: ${{ vars.DOWNLOADS_JSON }}
          DEBUG_MODE: ${{ github.event.inputs.debug }}
        run: |
          # -------------------------------------------------
          # Preparación general
          # -------------------------------------------------
          set -u -o pipefail               # falla en pipelines, pero no en comandos individuales
          if [ "$DEBUG_MODE" = "true" ]; then set -x; fi

          # -------------------------------------------------
          # Desactivar errexit mientras iteramos
          # -------------------------------------------------
          set +e                           # evita que cualquier comando con rc≠0 aborta el script

          echo "$DOWNLOADS_JSON" | jq -c '.[]' | while read -r item; do
            url=$(echo "$item" | jq -r '.url')
            dest=$(echo "$item" | jq -r '.dest')
            tmp="${dest}.tmp.$RANDOM"

            # Asegurar que exista el directorio destino
            mkdir -p "$(dirname "$dest")"

            echo "::group::Descargando $url → $tmp"
            #   --max-time 300  → timeout de 5 min
            #   --retry 5      → hasta 5 intentos
            #   --retry-delay 10 → 10 s entre intentos
            curl -fSL --max-time 300 --retry 5 --retry-delay 10 "$url" -o "$tmp"
            rc=$?                         # captura código de salida de curl
            echo "::endgroup::"

            # -------------------------------------------------
            # Manejo de errores (504, 502, 5xx, etc.)
            # -------------------------------------------------
            if [ $rc -ne 0 ]; then
              echo "❌  ERROR al descargar $url (código $rc). Se omite y se continúa."
              rm -f "$tmp"
              continue                    # pasa a la siguiente URL
            fi

            # -------------------------------------------------
            # Archivo temporal vacío → descartamos
            # -------------------------------------------------
            if [ ! -s "$tmp" ]; then
              echo "⚠️  Archivo temporal vacío; no sustituyo $dest."
              rm -f "$tmp"
              continue
            fi

            # -------------------------------------------------
            # (Opcional) checksum SHA‑256
            # -------------------------------------------------
            expected=$(echo "$item" | jq -r '.sha256 // empty')
            if [ -n "$expected" ]; then
              echo "$expected  $tmp" | sha256sum -c - >/dev/null || {
                echo "❌  Checksum falló para $url → $tmp. Archivo descartado."
                rm -f "$tmp"
                continue
              }
            fi

            # -------------------------------------------------
            # Todo correcto → mover al destino definitivo
            # -------------------------------------------------
            echo "✅  Descarga completada. Reemplazando $dest."
            mv -f "$tmp" "$dest"
          done

          # -------------------------------------------------
          # Reactivar errexit para el resto del job
          # -------------------------------------------------
          set -e

          # (opcional) resumen final
          echo "=== LISTADO FINAL DE downloads/ ==="
          find downloads/ -type f -print
          echo "===================================="

      # -------------------------------------------------
      # 3️⃣ Listar contenido final (para depuración)
      # -------------------------------------------------
      - name: Listar contenido de downloads/
        if: always()
        run: |
          echo "=== LISTADO FINAL DE downloads/ ==="
          find downloads/ -type f -print
          echo "===================================="

      # -------------------------------------------------
      # 4️⃣ Empaquetar y subir artefactos
      # -------------------------------------------------
      - name: Empaquetar artefactos
        run: tar -czf documentos-multiples.tar.gz downloads/

      - name: Subir artefactos
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: documentos-multiples
          path: documentos-multiples.tar.gz

      # -------------------------------------------------
      # 5️⃣ Commit y push (usa PAT almacenado en secret PAT_PUSH)
      # -------------------------------------------------
      - name: Commit y push de los archivos descargados
        if: success()
        env:
          GIT_TOKEN: ${{ secrets.PAT_PUSH }}   # <-- PAT con scope `repo` (o `public_repo`)
        run: |
          # Configurar usuario de commit
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"

          # Añadir cambios (solo los archivos bajo downloads/)
          git add -A downloads/

          # Si no hay cambios, salir sin crear commit
          if git diff-index --quiet HEAD; then
            echo "🔎  No se detectaron cambios en los archivos; no se crea commit."
            exit 0
          fi

          # Crear el commit
          git commit -m "📥 Descarga automática $(date +'%Y-%m-%d %H:%M')"

          # Push usando el PAT (se construye la URL con el token)
          remote_url="https://${GIT_TOKEN}@github.com/${{ github.repository }}.git"
          git remote set-url origin "$remote_url"

          # Push a la rama que disparó el workflow
          git push origin HEAD:${{ github.ref_name }}

          # Restaurar remote a la URL pública (buena práctica)
          git remote set-url origin "https://github.com/${{ github.repository }}.git"